/**
 * Module dependencies
 */

var logger = require('pomelo-logger').getLogger(__filename);

var exp = module.exports;

// global room container(roomId:roomObj)

var gRoomList = [];


// create new room, add the player to the room
exp.createRoom = function(id) {

};


